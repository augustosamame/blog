<% post.tags.each_with_index do |tag, index| %>
  <% if index == post.tags.size - 1 %>
    <%= link_to tag.name, root_path(:action => :main, :tag => tag.name) %>
  <% else %>
    <%= link_to tag.name, root_path(:action => :main, :tag => tag.name) %><%= separator %>
  <% end %>
<% end %>
